/* Copyright 2008 Misys PLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License"); 
 * you may not use this file except in compliance with the License. 
 * You may obtain a copy of the License at
 *
 *    http://www.apache.org/licenses/LICENSE-2.0 
 * 
 * Unless required by applicable law or agreed to in writing, software 
 * distributed under the License is distributed on an "AS IS" BASIS, 
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or 
 * implied. See the License for the specific language governing 
 * permissions and limitations under the License. 
 */
package com.misyshealthcare.connect.doc.ccd.xsd;


/**
 *  XDSMSData bean class
 */
public class XDSMSData extends com.misyshealthcare.connect.doc.ccd.xsd.CCDData
    implements org.apache.axis2.databinding.ADBBean {
    /**
     * field for ActiveProblems
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] localActiveProblems;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localActiveProblemsTracker = false;

    /**
     * field for AdvanceDirectives
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[] localAdvanceDirectives;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localAdvanceDirectivesTracker = false;

    /**
     * field for Allergies
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[] localAllergies;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localAllergiesTracker = false;

    /**
     * field for HistoryOfPresentIllness
     * This was an Array!
     */
    protected java.lang.String[] localHistoryOfPresentIllness;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localHistoryOfPresentIllnessTracker = false;

    /**
     * field for Insurances
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[] localInsurances;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localInsurancesTracker = false;

    /**
     * field for Measurements
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[] localMeasurements;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localMeasurementsTracker = false;

    /**
     * field for Medications
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[] localMedications;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localMedicationsTracker = false;

    /**
     * field for PhysicalExams
     * This was an Array!
     */
    protected java.lang.String[] localPhysicalExams;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localPhysicalExamsTracker = false;

    /**
     * field for PlanOfCares
     * This was an Array!
     */
    protected java.lang.String[] localPlanOfCares;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localPlanOfCaresTracker = false;

    /**
     * field for Procedures
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[] localProcedures;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localProceduresTracker = false;

    /**
     * field for ResolvedProblems
     * This was an Array!
     */
    protected com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] localResolvedProblems;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localResolvedProblemsTracker = false;

    /**
     * field for ReviewOfSystems
     * This was an Array!
     */
    protected java.lang.String[] localReviewOfSystems;

    /*  This tracker boolean wil be used to detect whether the user called the set method
     *   for this attribute. It will be used to determine whether to include this field
     *   in the serialized XML
     */
    protected boolean localReviewOfSystemsTracker = false;

    /* This type was generated from the piece of schema that had
       name = XDSMSData
       Namespace URI = http://ccd.doc.connect.misyshealthcare.com/xsd
       Namespace Prefix = ns1
     */
    private static java.lang.String generatePrefix(java.lang.String namespace) {
        if (namespace.equals("http://ccd.doc.connect.misyshealthcare.com/xsd")) {
            return "ns1";
        }

        return org.apache.axis2.databinding.utils.BeanUtil.getUniquePrefix();
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] getActiveProblems() {
        return localActiveProblems;
    }

    /**
     * validate the array for ActiveProblems
     */
    protected void validateActiveProblems(
        com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] param) {
    }

    /**
     * Auto generated setter method
     * @param param ActiveProblems
     */
    public void setActiveProblems(
        com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] param) {
        validateActiveProblems(param);

        if (param != null) {
            //update the setting tracker
            localActiveProblemsTracker = true;
        } else {
            localActiveProblemsTracker = true;
        }

        this.localActiveProblems = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem
     */
    public void addActiveProblems(
        com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem param) {
        if (localActiveProblems == null) {
            localActiveProblems = new com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] {
                    
                };
        }

        //update the setting tracker
        localActiveProblemsTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localActiveProblems);
        list.add(param);
        this.localActiveProblems = (com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[] getAdvanceDirectives() {
        return localAdvanceDirectives;
    }

    /**
     * validate the array for AdvanceDirectives
     */
    protected void validateAdvanceDirectives(
        com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[] param) {
    }

    /**
     * Auto generated setter method
     * @param param AdvanceDirectives
     */
    public void setAdvanceDirectives(
        com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[] param) {
        validateAdvanceDirectives(param);

        if (param != null) {
            //update the setting tracker
            localAdvanceDirectivesTracker = true;
        } else {
            localAdvanceDirectivesTracker = true;
        }

        this.localAdvanceDirectives = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective
     */
    public void addAdvanceDirectives(
        com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective param) {
        if (localAdvanceDirectives == null) {
            localAdvanceDirectives = new com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[] {
                    
                };
        }

        //update the setting tracker
        localAdvanceDirectivesTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localAdvanceDirectives);
        list.add(param);
        this.localAdvanceDirectives = (com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[] getAllergies() {
        return localAllergies;
    }

    /**
     * validate the array for Allergies
     */
    protected void validateAllergies(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[] param) {
    }

    /**
     * Auto generated setter method
     * @param param Allergies
     */
    public void setAllergies(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[] param) {
        validateAllergies(param);

        if (param != null) {
            //update the setting tracker
            localAllergiesTracker = true;
        } else {
            localAllergiesTracker = true;
        }

        this.localAllergies = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy
     */
    public void addAllergies(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy param) {
        if (localAllergies == null) {
            localAllergies = new com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[] {
                    
                };
        }

        //update the setting tracker
        localAllergiesTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localAllergies);
        list.add(param);
        this.localAllergies = (com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return java.lang.String[]
     */
    public java.lang.String[] getHistoryOfPresentIllness() {
        return localHistoryOfPresentIllness;
    }

    /**
     * validate the array for HistoryOfPresentIllness
     */
    protected void validateHistoryOfPresentIllness(java.lang.String[] param) {
    }

    /**
     * Auto generated setter method
     * @param param HistoryOfPresentIllness
     */
    public void setHistoryOfPresentIllness(java.lang.String[] param) {
        validateHistoryOfPresentIllness(param);

        if (param != null) {
            //update the setting tracker
            localHistoryOfPresentIllnessTracker = true;
        } else {
            localHistoryOfPresentIllnessTracker = true;
        }

        this.localHistoryOfPresentIllness = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param java.lang.String
     */
    public void addHistoryOfPresentIllness(java.lang.String param) {
        if (localHistoryOfPresentIllness == null) {
            localHistoryOfPresentIllness = new java.lang.String[] {  };
        }

        //update the setting tracker
        localHistoryOfPresentIllnessTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localHistoryOfPresentIllness);
        list.add(param);
        this.localHistoryOfPresentIllness = (java.lang.String[]) list.toArray(new java.lang.String[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[]
     */
    public com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[] getInsurances() {
        return localInsurances;
    }

    /**
     * validate the array for Insurances
     */
    protected void validateInsurances(
        com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[] param) {
    }

    /**
     * Auto generated setter method
     * @param param Insurances
     */
    public void setInsurances(
        com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[] param) {
        validateInsurances(param);

        if (param != null) {
            //update the setting tracker
            localInsurancesTracker = true;
        } else {
            localInsurancesTracker = true;
        }

        this.localInsurances = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.demographicdata.xsd.Insurance
     */
    public void addInsurances(
        com.misyshealthcare.connect.base.demographicdata.xsd.Insurance param) {
        if (localInsurances == null) {
            localInsurances = new com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[] {
                    
                };
        }

        //update the setting tracker
        localInsurancesTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localInsurances);
        list.add(param);
        this.localInsurances = (com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[]) list.toArray(new com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[] getMeasurements() {
        return localMeasurements;
    }

    /**
     * validate the array for Measurements
     */
    protected void validateMeasurements(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[] param) {
    }

    /**
     * Auto generated setter method
     * @param param Measurements
     */
    public void setMeasurements(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[] param) {
        validateMeasurements(param);

        if (param != null) {
            //update the setting tracker
            localMeasurementsTracker = true;
        } else {
            localMeasurementsTracker = true;
        }

        this.localMeasurements = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements
     */
    public void addMeasurements(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements param) {
        if (localMeasurements == null) {
            localMeasurements = new com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[] {
                    
                };
        }

        //update the setting tracker
        localMeasurementsTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localMeasurements);
        list.add(param);
        this.localMeasurements = (com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[] getMedications() {
        return localMedications;
    }

    /**
     * validate the array for Medications
     */
    protected void validateMedications(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[] param) {
    }

    /**
     * Auto generated setter method
     * @param param Medications
     */
    public void setMedications(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[] param) {
        validateMedications(param);

        if (param != null) {
            //update the setting tracker
            localMedicationsTracker = true;
        } else {
            localMedicationsTracker = true;
        }

        this.localMedications = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.Medication
     */
    public void addMedications(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Medication param) {
        if (localMedications == null) {
            localMedications = new com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[] {
                    
                };
        }

        //update the setting tracker
        localMedicationsTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localMedications);
        list.add(param);
        this.localMedications = (com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return java.lang.String[]
     */
    public java.lang.String[] getPhysicalExams() {
        return localPhysicalExams;
    }

    /**
     * validate the array for PhysicalExams
     */
    protected void validatePhysicalExams(java.lang.String[] param) {
    }

    /**
     * Auto generated setter method
     * @param param PhysicalExams
     */
    public void setPhysicalExams(java.lang.String[] param) {
        validatePhysicalExams(param);

        if (param != null) {
            //update the setting tracker
            localPhysicalExamsTracker = true;
        } else {
            localPhysicalExamsTracker = true;
        }

        this.localPhysicalExams = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param java.lang.String
     */
    public void addPhysicalExams(java.lang.String param) {
        if (localPhysicalExams == null) {
            localPhysicalExams = new java.lang.String[] {  };
        }

        //update the setting tracker
        localPhysicalExamsTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localPhysicalExams);
        list.add(param);
        this.localPhysicalExams = (java.lang.String[]) list.toArray(new java.lang.String[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return java.lang.String[]
     */
    public java.lang.String[] getPlanOfCares() {
        return localPlanOfCares;
    }

    /**
     * validate the array for PlanOfCares
     */
    protected void validatePlanOfCares(java.lang.String[] param) {
    }

    /**
     * Auto generated setter method
     * @param param PlanOfCares
     */
    public void setPlanOfCares(java.lang.String[] param) {
        validatePlanOfCares(param);

        if (param != null) {
            //update the setting tracker
            localPlanOfCaresTracker = true;
        } else {
            localPlanOfCaresTracker = true;
        }

        this.localPlanOfCares = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param java.lang.String
     */
    public void addPlanOfCares(java.lang.String param) {
        if (localPlanOfCares == null) {
            localPlanOfCares = new java.lang.String[] {  };
        }

        //update the setting tracker
        localPlanOfCaresTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localPlanOfCares);
        list.add(param);
        this.localPlanOfCares = (java.lang.String[]) list.toArray(new java.lang.String[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[] getProcedures() {
        return localProcedures;
    }

    /**
     * validate the array for Procedures
     */
    protected void validateProcedures(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[] param) {
    }

    /**
     * Auto generated setter method
     * @param param Procedures
     */
    public void setProcedures(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[] param) {
        validateProcedures(param);

        if (param != null) {
            //update the setting tracker
            localProceduresTracker = true;
        } else {
            localProceduresTracker = true;
        }

        this.localProcedures = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure
     */
    public void addProcedures(
        com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure param) {
        if (localProcedures == null) {
            localProcedures = new com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[] {
                    
                };
        }

        //update the setting tracker
        localProceduresTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localProcedures);
        list.add(param);
        this.localProcedures = (com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[]
     */
    public com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] getResolvedProblems() {
        return localResolvedProblems;
    }

    /**
     * validate the array for ResolvedProblems
     */
    protected void validateResolvedProblems(
        com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] param) {
    }

    /**
     * Auto generated setter method
     * @param param ResolvedProblems
     */
    public void setResolvedProblems(
        com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] param) {
        validateResolvedProblems(param);

        if (param != null) {
            //update the setting tracker
            localResolvedProblemsTracker = true;
        } else {
            localResolvedProblemsTracker = true;
        }

        this.localResolvedProblems = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem
     */
    public void addResolvedProblems(
        com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem param) {
        if (localResolvedProblems == null) {
            localResolvedProblems = new com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[] {
                    
                };
        }

        //update the setting tracker
        localResolvedProblemsTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localResolvedProblems);
        list.add(param);
        this.localResolvedProblems = (com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[]) list.toArray(new com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[list.size()]);
    }

    /**
     * Auto generated getter method
     * @return java.lang.String[]
     */
    public java.lang.String[] getReviewOfSystems() {
        return localReviewOfSystems;
    }

    /**
     * validate the array for ReviewOfSystems
     */
    protected void validateReviewOfSystems(java.lang.String[] param) {
    }

    /**
     * Auto generated setter method
     * @param param ReviewOfSystems
     */
    public void setReviewOfSystems(java.lang.String[] param) {
        validateReviewOfSystems(param);

        if (param != null) {
            //update the setting tracker
            localReviewOfSystemsTracker = true;
        } else {
            localReviewOfSystemsTracker = true;
        }

        this.localReviewOfSystems = param;
    }

    /**
     * Auto generated add method for the array for convenience
     * @param param java.lang.String
     */
    public void addReviewOfSystems(java.lang.String param) {
        if (localReviewOfSystems == null) {
            localReviewOfSystems = new java.lang.String[] {  };
        }

        //update the setting tracker
        localReviewOfSystemsTracker = true;

        java.util.List list = org.apache.axis2.databinding.utils.ConverterUtil.toList(localReviewOfSystems);
        list.add(param);
        this.localReviewOfSystems = (java.lang.String[]) list.toArray(new java.lang.String[list.size()]);
    }

    /**
     * isReaderMTOMAware
     * @return true if the reader supports MTOM
     */
    public static boolean isReaderMTOMAware(
        javax.xml.stream.XMLStreamReader reader) {
        boolean isReaderMTOMAware = false;

        try {
            isReaderMTOMAware = java.lang.Boolean.TRUE.equals(reader.getProperty(
                        org.apache.axiom.om.OMConstants.IS_DATA_HANDLERS_AWARE));
        } catch (java.lang.IllegalArgumentException e) {
            isReaderMTOMAware = false;
        }

        return isReaderMTOMAware;
    }

    /**
     *
     * @param parentQName
     * @param factory
     * @return org.apache.axiom.om.OMElement
     */
    public org.apache.axiom.om.OMElement getOMElement(
        final javax.xml.namespace.QName parentQName,
        final org.apache.axiom.om.OMFactory factory)
        throws org.apache.axis2.databinding.ADBException {
        org.apache.axiom.om.OMDataSource dataSource = new org.apache.axis2.databinding.ADBDataSource(this,
                parentQName) {
                public void serialize(
                    org.apache.axis2.databinding.utils.writer.MTOMAwareXMLStreamWriter xmlWriter)
                    throws javax.xml.stream.XMLStreamException {
                    XDSMSData.this.serialize(parentQName, factory, xmlWriter);
                }
            };

        return new org.apache.axiom.om.impl.llom.OMSourcedElementImpl(parentQName,
            factory, dataSource);
    }

    public void serialize(final javax.xml.namespace.QName parentQName,
        final org.apache.axiom.om.OMFactory factory,
        org.apache.axis2.databinding.utils.writer.MTOMAwareXMLStreamWriter xmlWriter)
        throws javax.xml.stream.XMLStreamException,
            org.apache.axis2.databinding.ADBException {
        java.lang.String prefix = null;
        java.lang.String namespace = null;

        prefix = parentQName.getPrefix();
        namespace = parentQName.getNamespaceURI();

        if (namespace != null) {
            java.lang.String writerPrefix = xmlWriter.getPrefix(namespace);

            if (writerPrefix != null) {
                xmlWriter.writeStartElement(namespace,
                    parentQName.getLocalPart());
            } else {
                if (prefix == null) {
                    prefix = generatePrefix(namespace);
                }

                xmlWriter.writeStartElement(prefix, parentQName.getLocalPart(),
                    namespace);
                xmlWriter.writeNamespace(prefix, namespace);
                xmlWriter.setPrefix(prefix, namespace);
            }
        } else {
            xmlWriter.writeStartElement(parentQName.getLocalPart());
        }

        java.lang.String namespacePrefix = registerPrefix(xmlWriter,
                "http://ccd.doc.connect.misyshealthcare.com/xsd");

        if ((namespacePrefix != null) && (namespacePrefix.trim().length() > 0)) {
            writeAttribute("xsi", "http://www.w3.org/2001/XMLSchema-instance",
                "type", namespacePrefix + ":XDSMSData", xmlWriter);
        } else {
            writeAttribute("xsi", "http://www.w3.org/2001/XMLSchema-instance",
                "type", "XDSMSData", xmlWriter);
        }

        if (localMetadataTracker) {
            if (localMetadata == null) {
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "metadata",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "metadata");
                    }
                } else {
                    xmlWriter.writeStartElement("metadata");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            } else {
                localMetadata.serialize(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "metadata"), factory, xmlWriter);
            }
        }

        if (localActiveProblemsTracker) {
            if (localActiveProblems != null) {
                for (int i = 0; i < localActiveProblems.length; i++) {
                    if (localActiveProblems[i] != null) {
                        localActiveProblems[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "activeProblems"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "activeProblems", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "activeProblems");
                            }
                        } else {
                            xmlWriter.writeStartElement("activeProblems");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "activeProblems",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "activeProblems");
                    }
                } else {
                    xmlWriter.writeStartElement("activeProblems");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localAdvanceDirectivesTracker) {
            if (localAdvanceDirectives != null) {
                for (int i = 0; i < localAdvanceDirectives.length; i++) {
                    if (localAdvanceDirectives[i] != null) {
                        localAdvanceDirectives[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "advanceDirectives"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "advanceDirectives", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "advanceDirectives");
                            }
                        } else {
                            xmlWriter.writeStartElement("advanceDirectives");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2,
                            "advanceDirectives", namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2,
                            "advanceDirectives");
                    }
                } else {
                    xmlWriter.writeStartElement("advanceDirectives");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localAllergiesTracker) {
            if (localAllergies != null) {
                for (int i = 0; i < localAllergies.length; i++) {
                    if (localAllergies[i] != null) {
                        localAllergies[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "allergies"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "allergies", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "allergies");
                            }
                        } else {
                            xmlWriter.writeStartElement("allergies");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "allergies",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "allergies");
                    }
                } else {
                    xmlWriter.writeStartElement("allergies");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localHistoryOfPresentIllnessTracker) {
            if (localHistoryOfPresentIllness != null) {
                namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                boolean emptyNamespace = (namespace == null) ||
                    (namespace.length() == 0);
                prefix = emptyNamespace ? null : xmlWriter.getPrefix(namespace);

                for (int i = 0; i < localHistoryOfPresentIllness.length; i++) {
                    if (localHistoryOfPresentIllness[i] != null) {
                        if (!emptyNamespace) {
                            if (prefix == null) {
                                java.lang.String prefix2 = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix2,
                                    "historyOfPresentIllness", namespace);
                                xmlWriter.writeNamespace(prefix2, namespace);
                                xmlWriter.setPrefix(prefix2, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "historyOfPresentIllness");
                            }
                        } else {
                            xmlWriter.writeStartElement(
                                "historyOfPresentIllness");
                        }

                        xmlWriter.writeCharacters(localHistoryOfPresentIllness[i]);

                        xmlWriter.writeEndElement();
                    } else {
                        // write null attribute
                        namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace.equals("")) {
                            prefix = xmlWriter.getPrefix(namespace);

                            if (prefix == null) {
                                prefix = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix,
                                    "historyOfPresentIllness", namespace);
                                xmlWriter.writeNamespace(prefix, namespace);
                                xmlWriter.setPrefix(prefix, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "historyOfPresentIllness");
                            }
                        } else {
                            xmlWriter.writeStartElement(
                                "historyOfPresentIllness");
                        }

                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write the null attribute
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2,
                            "historyOfPresentIllness", namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2,
                            "historyOfPresentIllness");
                    }
                } else {
                    xmlWriter.writeStartElement("historyOfPresentIllness");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localInsurancesTracker) {
            if (localInsurances != null) {
                for (int i = 0; i < localInsurances.length; i++) {
                    if (localInsurances[i] != null) {
                        localInsurances[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "insurances"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "insurances", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "insurances");
                            }
                        } else {
                            xmlWriter.writeStartElement("insurances");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "insurances",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "insurances");
                    }
                } else {
                    xmlWriter.writeStartElement("insurances");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localMeasurementsTracker) {
            if (localMeasurements != null) {
                for (int i = 0; i < localMeasurements.length; i++) {
                    if (localMeasurements[i] != null) {
                        localMeasurements[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "measurements"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "measurements", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "measurements");
                            }
                        } else {
                            xmlWriter.writeStartElement("measurements");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "measurements",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "measurements");
                    }
                } else {
                    xmlWriter.writeStartElement("measurements");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localMedicationsTracker) {
            if (localMedications != null) {
                for (int i = 0; i < localMedications.length; i++) {
                    if (localMedications[i] != null) {
                        localMedications[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "medications"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "medications", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "medications");
                            }
                        } else {
                            xmlWriter.writeStartElement("medications");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "medications",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "medications");
                    }
                } else {
                    xmlWriter.writeStartElement("medications");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localPhysicalExamsTracker) {
            if (localPhysicalExams != null) {
                namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                boolean emptyNamespace = (namespace == null) ||
                    (namespace.length() == 0);
                prefix = emptyNamespace ? null : xmlWriter.getPrefix(namespace);

                for (int i = 0; i < localPhysicalExams.length; i++) {
                    if (localPhysicalExams[i] != null) {
                        if (!emptyNamespace) {
                            if (prefix == null) {
                                java.lang.String prefix2 = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix2,
                                    "physicalExams", namespace);
                                xmlWriter.writeNamespace(prefix2, namespace);
                                xmlWriter.setPrefix(prefix2, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "physicalExams");
                            }
                        } else {
                            xmlWriter.writeStartElement("physicalExams");
                        }

                        xmlWriter.writeCharacters(localPhysicalExams[i]);

                        xmlWriter.writeEndElement();
                    } else {
                        // write null attribute
                        namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace.equals("")) {
                            prefix = xmlWriter.getPrefix(namespace);

                            if (prefix == null) {
                                prefix = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix,
                                    "physicalExams", namespace);
                                xmlWriter.writeNamespace(prefix, namespace);
                                xmlWriter.setPrefix(prefix, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "physicalExams");
                            }
                        } else {
                            xmlWriter.writeStartElement("physicalExams");
                        }

                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write the null attribute
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "physicalExams",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "physicalExams");
                    }
                } else {
                    xmlWriter.writeStartElement("physicalExams");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localPlanOfCaresTracker) {
            if (localPlanOfCares != null) {
                namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                boolean emptyNamespace = (namespace == null) ||
                    (namespace.length() == 0);
                prefix = emptyNamespace ? null : xmlWriter.getPrefix(namespace);

                for (int i = 0; i < localPlanOfCares.length; i++) {
                    if (localPlanOfCares[i] != null) {
                        if (!emptyNamespace) {
                            if (prefix == null) {
                                java.lang.String prefix2 = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix2,
                                    "planOfCares", namespace);
                                xmlWriter.writeNamespace(prefix2, namespace);
                                xmlWriter.setPrefix(prefix2, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "planOfCares");
                            }
                        } else {
                            xmlWriter.writeStartElement("planOfCares");
                        }

                        xmlWriter.writeCharacters(localPlanOfCares[i]);

                        xmlWriter.writeEndElement();
                    } else {
                        // write null attribute
                        namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace.equals("")) {
                            prefix = xmlWriter.getPrefix(namespace);

                            if (prefix == null) {
                                prefix = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix,
                                    "planOfCares", namespace);
                                xmlWriter.writeNamespace(prefix, namespace);
                                xmlWriter.setPrefix(prefix, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "planOfCares");
                            }
                        } else {
                            xmlWriter.writeStartElement("planOfCares");
                        }

                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write the null attribute
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "planOfCares",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "planOfCares");
                    }
                } else {
                    xmlWriter.writeStartElement("planOfCares");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localProceduresTracker) {
            if (localProcedures != null) {
                for (int i = 0; i < localProcedures.length; i++) {
                    if (localProcedures[i] != null) {
                        localProcedures[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "procedures"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "procedures", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "procedures");
                            }
                        } else {
                            xmlWriter.writeStartElement("procedures");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "procedures",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2, "procedures");
                    }
                } else {
                    xmlWriter.writeStartElement("procedures");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localResolvedProblemsTracker) {
            if (localResolvedProblems != null) {
                for (int i = 0; i < localResolvedProblems.length; i++) {
                    if (localResolvedProblems[i] != null) {
                        localResolvedProblems[i].serialize(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "resolvedProblems"), factory, xmlWriter);
                    } else {
                        // write null attribute
                        java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace2.equals("")) {
                            java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                            if (prefix2 == null) {
                                prefix2 = generatePrefix(namespace2);

                                xmlWriter.writeStartElement(prefix2,
                                    "resolvedProblems", namespace2);
                                xmlWriter.writeNamespace(prefix2, namespace2);
                                xmlWriter.setPrefix(prefix2, namespace2);
                            } else {
                                xmlWriter.writeStartElement(namespace2,
                                    "resolvedProblems");
                            }
                        } else {
                            xmlWriter.writeStartElement("resolvedProblems");
                        }

                        // write the nil attribute
                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2,
                            "resolvedProblems", namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2,
                            "resolvedProblems");
                    }
                } else {
                    xmlWriter.writeStartElement("resolvedProblems");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        if (localReviewOfSystemsTracker) {
            if (localReviewOfSystems != null) {
                namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                boolean emptyNamespace = (namespace == null) ||
                    (namespace.length() == 0);
                prefix = emptyNamespace ? null : xmlWriter.getPrefix(namespace);

                for (int i = 0; i < localReviewOfSystems.length; i++) {
                    if (localReviewOfSystems[i] != null) {
                        if (!emptyNamespace) {
                            if (prefix == null) {
                                java.lang.String prefix2 = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix2,
                                    "reviewOfSystems", namespace);
                                xmlWriter.writeNamespace(prefix2, namespace);
                                xmlWriter.setPrefix(prefix2, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "reviewOfSystems");
                            }
                        } else {
                            xmlWriter.writeStartElement("reviewOfSystems");
                        }

                        xmlWriter.writeCharacters(localReviewOfSystems[i]);

                        xmlWriter.writeEndElement();
                    } else {
                        // write null attribute
                        namespace = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                        if (!namespace.equals("")) {
                            prefix = xmlWriter.getPrefix(namespace);

                            if (prefix == null) {
                                prefix = generatePrefix(namespace);

                                xmlWriter.writeStartElement(prefix,
                                    "reviewOfSystems", namespace);
                                xmlWriter.writeNamespace(prefix, namespace);
                                xmlWriter.setPrefix(prefix, namespace);
                            } else {
                                xmlWriter.writeStartElement(namespace,
                                    "reviewOfSystems");
                            }
                        } else {
                            xmlWriter.writeStartElement("reviewOfSystems");
                        }

                        writeAttribute("xsi",
                            "http://www.w3.org/2001/XMLSchema-instance", "nil",
                            "1", xmlWriter);
                        xmlWriter.writeEndElement();
                    }
                }
            } else {
                // write the null attribute
                // write null attribute
                java.lang.String namespace2 = "http://ccd.doc.connect.misyshealthcare.com/xsd";

                if (!namespace2.equals("")) {
                    java.lang.String prefix2 = xmlWriter.getPrefix(namespace2);

                    if (prefix2 == null) {
                        prefix2 = generatePrefix(namespace2);

                        xmlWriter.writeStartElement(prefix2, "reviewOfSystems",
                            namespace2);
                        xmlWriter.writeNamespace(prefix2, namespace2);
                        xmlWriter.setPrefix(prefix2, namespace2);
                    } else {
                        xmlWriter.writeStartElement(namespace2,
                            "reviewOfSystems");
                    }
                } else {
                    xmlWriter.writeStartElement("reviewOfSystems");
                }

                // write the nil attribute
                writeAttribute("xsi",
                    "http://www.w3.org/2001/XMLSchema-instance", "nil", "1",
                    xmlWriter);
                xmlWriter.writeEndElement();
            }
        }

        xmlWriter.writeEndElement();
    }

    /**
     * Util method to write an attribute with the ns prefix
     */
    private void writeAttribute(java.lang.String prefix,
        java.lang.String namespace, java.lang.String attName,
        java.lang.String attValue, javax.xml.stream.XMLStreamWriter xmlWriter)
        throws javax.xml.stream.XMLStreamException {
        if (xmlWriter.getPrefix(namespace) == null) {
            xmlWriter.writeNamespace(prefix, namespace);
            xmlWriter.setPrefix(prefix, namespace);
        }

        xmlWriter.writeAttribute(namespace, attName, attValue);
    }

    /**
     * Util method to write an attribute without the ns prefix
     */
    private void writeAttribute(java.lang.String namespace,
        java.lang.String attName, java.lang.String attValue,
        javax.xml.stream.XMLStreamWriter xmlWriter)
        throws javax.xml.stream.XMLStreamException {
        if (namespace.equals("")) {
            xmlWriter.writeAttribute(attName, attValue);
        } else {
            registerPrefix(xmlWriter, namespace);
            xmlWriter.writeAttribute(namespace, attName, attValue);
        }
    }

    /**
     * Util method to write an attribute without the ns prefix
     */
    private void writeQNameAttribute(java.lang.String namespace,
        java.lang.String attName, javax.xml.namespace.QName qname,
        javax.xml.stream.XMLStreamWriter xmlWriter)
        throws javax.xml.stream.XMLStreamException {
        java.lang.String attributeNamespace = qname.getNamespaceURI();
        java.lang.String attributePrefix = xmlWriter.getPrefix(attributeNamespace);

        if (attributePrefix == null) {
            attributePrefix = registerPrefix(xmlWriter, attributeNamespace);
        }

        java.lang.String attributeValue;

        if (attributePrefix.trim().length() > 0) {
            attributeValue = attributePrefix + ":" + qname.getLocalPart();
        } else {
            attributeValue = qname.getLocalPart();
        }

        if (namespace.equals("")) {
            xmlWriter.writeAttribute(attName, attributeValue);
        } else {
            registerPrefix(xmlWriter, namespace);
            xmlWriter.writeAttribute(namespace, attName, attributeValue);
        }
    }

    /**
     *  method to handle Qnames
     */
    private void writeQName(javax.xml.namespace.QName qname,
        javax.xml.stream.XMLStreamWriter xmlWriter)
        throws javax.xml.stream.XMLStreamException {
        java.lang.String namespaceURI = qname.getNamespaceURI();

        if (namespaceURI != null) {
            java.lang.String prefix = xmlWriter.getPrefix(namespaceURI);

            if (prefix == null) {
                prefix = generatePrefix(namespaceURI);
                xmlWriter.writeNamespace(prefix, namespaceURI);
                xmlWriter.setPrefix(prefix, namespaceURI);
            }

            if (prefix.trim().length() > 0) {
                xmlWriter.writeCharacters(prefix + ":" +
                    org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                        qname));
            } else {
                // i.e this is the default namespace
                xmlWriter.writeCharacters(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                        qname));
            }
        } else {
            xmlWriter.writeCharacters(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                    qname));
        }
    }

    private void writeQNames(javax.xml.namespace.QName[] qnames,
        javax.xml.stream.XMLStreamWriter xmlWriter)
        throws javax.xml.stream.XMLStreamException {
        if (qnames != null) {
            // we have to store this data until last moment since it is not possible to write any
            // namespace data after writing the charactor data
            java.lang.StringBuffer stringToWrite = new java.lang.StringBuffer();
            java.lang.String namespaceURI = null;
            java.lang.String prefix = null;

            for (int i = 0; i < qnames.length; i++) {
                if (i > 0) {
                    stringToWrite.append(" ");
                }

                namespaceURI = qnames[i].getNamespaceURI();

                if (namespaceURI != null) {
                    prefix = xmlWriter.getPrefix(namespaceURI);

                    if ((prefix == null) || (prefix.length() == 0)) {
                        prefix = generatePrefix(namespaceURI);
                        xmlWriter.writeNamespace(prefix, namespaceURI);
                        xmlWriter.setPrefix(prefix, namespaceURI);
                    }

                    if (prefix.trim().length() > 0) {
                        stringToWrite.append(prefix).append(":")
                                     .append(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                                qnames[i]));
                    } else {
                        stringToWrite.append(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                                qnames[i]));
                    }
                } else {
                    stringToWrite.append(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                            qnames[i]));
                }
            }

            xmlWriter.writeCharacters(stringToWrite.toString());
        }
    }

    /**
     * Register a namespace prefix
     */
    private java.lang.String registerPrefix(
        javax.xml.stream.XMLStreamWriter xmlWriter, java.lang.String namespace)
        throws javax.xml.stream.XMLStreamException {
        java.lang.String prefix = xmlWriter.getPrefix(namespace);

        if (prefix == null) {
            prefix = generatePrefix(namespace);

            while (xmlWriter.getNamespaceContext().getNamespaceURI(prefix) != null) {
                prefix = org.apache.axis2.databinding.utils.BeanUtil.getUniquePrefix();
            }

            xmlWriter.writeNamespace(prefix, namespace);
            xmlWriter.setPrefix(prefix, namespace);
        }

        return prefix;
    }

    /**
     * databinding method to get an XML representation of this object
     *
     */
    public javax.xml.stream.XMLStreamReader getPullParser(
        javax.xml.namespace.QName qName)
        throws org.apache.axis2.databinding.ADBException {
        java.util.ArrayList elementList = new java.util.ArrayList();
        java.util.ArrayList attribList = new java.util.ArrayList();

        attribList.add(new javax.xml.namespace.QName(
                "http://www.w3.org/2001/XMLSchema-instance", "type"));
        attribList.add(new javax.xml.namespace.QName(
                "http://ccd.doc.connect.misyshealthcare.com/xsd", "XDSMSData"));

        if (localMetadataTracker) {
            elementList.add(new javax.xml.namespace.QName(
                    "http://ccd.doc.connect.misyshealthcare.com/xsd", "metadata"));

            elementList.add((localMetadata == null) ? null : localMetadata);
        }

        if (localActiveProblemsTracker) {
            if (localActiveProblems != null) {
                for (int i = 0; i < localActiveProblems.length; i++) {
                    if (localActiveProblems[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "activeProblems"));
                        elementList.add(localActiveProblems[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "activeProblems"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "activeProblems"));
                elementList.add(localActiveProblems);
            }
        }

        if (localAdvanceDirectivesTracker) {
            if (localAdvanceDirectives != null) {
                for (int i = 0; i < localAdvanceDirectives.length; i++) {
                    if (localAdvanceDirectives[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "advanceDirectives"));
                        elementList.add(localAdvanceDirectives[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "advanceDirectives"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "advanceDirectives"));
                elementList.add(localAdvanceDirectives);
            }
        }

        if (localAllergiesTracker) {
            if (localAllergies != null) {
                for (int i = 0; i < localAllergies.length; i++) {
                    if (localAllergies[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "allergies"));
                        elementList.add(localAllergies[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "allergies"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "allergies"));
                elementList.add(localAllergies);
            }
        }

        if (localHistoryOfPresentIllnessTracker) {
            if (localHistoryOfPresentIllness != null) {
                for (int i = 0; i < localHistoryOfPresentIllness.length; i++) {
                    if (localHistoryOfPresentIllness[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "historyOfPresentIllness"));
                        elementList.add(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                                localHistoryOfPresentIllness[i]));
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "historyOfPresentIllness"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "historyOfPresentIllness"));
                elementList.add(null);
            }
        }

        if (localInsurancesTracker) {
            if (localInsurances != null) {
                for (int i = 0; i < localInsurances.length; i++) {
                    if (localInsurances[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "insurances"));
                        elementList.add(localInsurances[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "insurances"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "insurances"));
                elementList.add(localInsurances);
            }
        }

        if (localMeasurementsTracker) {
            if (localMeasurements != null) {
                for (int i = 0; i < localMeasurements.length; i++) {
                    if (localMeasurements[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "measurements"));
                        elementList.add(localMeasurements[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "measurements"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "measurements"));
                elementList.add(localMeasurements);
            }
        }

        if (localMedicationsTracker) {
            if (localMedications != null) {
                for (int i = 0; i < localMedications.length; i++) {
                    if (localMedications[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "medications"));
                        elementList.add(localMedications[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "medications"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "medications"));
                elementList.add(localMedications);
            }
        }

        if (localPhysicalExamsTracker) {
            if (localPhysicalExams != null) {
                for (int i = 0; i < localPhysicalExams.length; i++) {
                    if (localPhysicalExams[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "physicalExams"));
                        elementList.add(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                                localPhysicalExams[i]));
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "physicalExams"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "physicalExams"));
                elementList.add(null);
            }
        }

        if (localPlanOfCaresTracker) {
            if (localPlanOfCares != null) {
                for (int i = 0; i < localPlanOfCares.length; i++) {
                    if (localPlanOfCares[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "planOfCares"));
                        elementList.add(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                                localPlanOfCares[i]));
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "planOfCares"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "planOfCares"));
                elementList.add(null);
            }
        }

        if (localProceduresTracker) {
            if (localProcedures != null) {
                for (int i = 0; i < localProcedures.length; i++) {
                    if (localProcedures[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "procedures"));
                        elementList.add(localProcedures[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "procedures"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "procedures"));
                elementList.add(localProcedures);
            }
        }

        if (localResolvedProblemsTracker) {
            if (localResolvedProblems != null) {
                for (int i = 0; i < localResolvedProblems.length; i++) {
                    if (localResolvedProblems[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "resolvedProblems"));
                        elementList.add(localResolvedProblems[i]);
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "resolvedProblems"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "resolvedProblems"));
                elementList.add(localResolvedProblems);
            }
        }

        if (localReviewOfSystemsTracker) {
            if (localReviewOfSystems != null) {
                for (int i = 0; i < localReviewOfSystems.length; i++) {
                    if (localReviewOfSystems[i] != null) {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "reviewOfSystems"));
                        elementList.add(org.apache.axis2.databinding.utils.ConverterUtil.convertToString(
                                localReviewOfSystems[i]));
                    } else {
                        elementList.add(new javax.xml.namespace.QName(
                                "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                "reviewOfSystems"));
                        elementList.add(null);
                    }
                }
            } else {
                elementList.add(new javax.xml.namespace.QName(
                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                        "reviewOfSystems"));
                elementList.add(null);
            }
        }

        return new org.apache.axis2.databinding.utils.reader.ADBXMLStreamReaderImpl(qName,
            elementList.toArray(), attribList.toArray());
    }

    /**
     *  Factory class that keeps the parse method
     */
    public static class Factory {
        /**
         * static method to create the object
         * Precondition:  If this object is an element, the current or next start element starts this object and any intervening reader events are ignorable
         *                If this object is not an element, it is a complex type and the reader is at the event just after the outer start element
         * Postcondition: If this object is an element, the reader is positioned at its end element
         *                If this object is a complex type, the reader is positioned at the end element of its outer element
         */
        public static XDSMSData parse(javax.xml.stream.XMLStreamReader reader)
            throws java.lang.Exception {
            XDSMSData object = new XDSMSData();

            int event;
            java.lang.String nillableValue = null;
            java.lang.String prefix = "";
            java.lang.String namespaceuri = "";

            try {
                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.getAttributeValue(
                            "http://www.w3.org/2001/XMLSchema-instance", "type") != null) {
                    java.lang.String fullTypeName = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "type");

                    if (fullTypeName != null) {
                        java.lang.String nsPrefix = null;

                        if (fullTypeName.indexOf(":") > -1) {
                            nsPrefix = fullTypeName.substring(0,
                                    fullTypeName.indexOf(":"));
                        }

                        nsPrefix = (nsPrefix == null) ? "" : nsPrefix;

                        java.lang.String type = fullTypeName.substring(fullTypeName.indexOf(
                                    ":") + 1);

                        if (!"XDSMSData".equals(type)) {
                            //find namespace for the prefix
                            java.lang.String nsUri = reader.getNamespaceContext()
                                                           .getNamespaceURI(nsPrefix);

                            return (XDSMSData) com.misyshealthcare.connect.doc.ccd.ExtensionMapper.getTypeObject(nsUri,
                                type, reader);
                        }
                    }
                }

                // Note all attributes that were handled. Used to differ normal attributes
                // from anyAttributes.
                java.util.Vector handledAttributes = new java.util.Vector();

                reader.next();

                java.util.ArrayList list2 = new java.util.ArrayList();

                java.util.ArrayList list3 = new java.util.ArrayList();

                java.util.ArrayList list4 = new java.util.ArrayList();

                java.util.ArrayList list5 = new java.util.ArrayList();

                java.util.ArrayList list6 = new java.util.ArrayList();

                java.util.ArrayList list7 = new java.util.ArrayList();

                java.util.ArrayList list8 = new java.util.ArrayList();

                java.util.ArrayList list9 = new java.util.ArrayList();

                java.util.ArrayList list10 = new java.util.ArrayList();

                java.util.ArrayList list11 = new java.util.ArrayList();

                java.util.ArrayList list12 = new java.util.ArrayList();

                java.util.ArrayList list13 = new java.util.ArrayList();

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "metadata").equals(reader.getName())) {
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        object.setMetadata(null);
                        reader.next();

                        reader.next();
                    } else {
                        object.setMetadata(com.misyshealthcare.connect.doc.ccd.xsd.MetaData.Factory.parse(
                                reader));

                        reader.next();
                    }
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "activeProblems").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list2.add(null);
                        reader.next();
                    } else {
                        list2.add(com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone2 = false;

                    while (!loopDone2) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone2 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "activeProblems").equals(
                                        reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list2.add(null);
                                    reader.next();
                                } else {
                                    list2.add(com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone2 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setActiveProblems((com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem.class,
                            list2));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "advanceDirectives").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list3.add(null);
                        reader.next();
                    } else {
                        list3.add(com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone3 = false;

                    while (!loopDone3) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone3 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "advanceDirectives").equals(
                                        reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list3.add(null);
                                    reader.next();
                                } else {
                                    list3.add(com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone3 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setAdvanceDirectives((com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.AdvanceDirective.class,
                            list3));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "allergies").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list4.add(null);
                        reader.next();
                    } else {
                        list4.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone4 = false;

                    while (!loopDone4) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone4 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "allergies").equals(reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list4.add(null);
                                    reader.next();
                                } else {
                                    list4.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone4 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setAllergies((com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.Allergy.class,
                            list4));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "historyOfPresentIllness").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list5.add(null);

                        reader.next();
                    } else {
                        list5.add(reader.getElementText());
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone5 = false;

                    while (!loopDone5) {
                        // Ensure we are at the EndElement
                        while (!reader.isEndElement()) {
                            reader.next();
                        }

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone5 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "historyOfPresentIllness").equals(
                                        reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list5.add(null);

                                    reader.next();
                                } else {
                                    list5.add(reader.getElementText());
                                }
                            } else {
                                loopDone5 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setHistoryOfPresentIllness((java.lang.String[]) list5.toArray(
                            new java.lang.String[list5.size()]));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "insurances").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list6.add(null);
                        reader.next();
                    } else {
                        list6.add(com.misyshealthcare.connect.base.demographicdata.xsd.Insurance.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone6 = false;

                    while (!loopDone6) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone6 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "insurances").equals(reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list6.add(null);
                                    reader.next();
                                } else {
                                    list6.add(com.misyshealthcare.connect.base.demographicdata.xsd.Insurance.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone6 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setInsurances((com.misyshealthcare.connect.base.demographicdata.xsd.Insurance[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.demographicdata.xsd.Insurance.class,
                            list6));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "measurements").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list7.add(null);
                        reader.next();
                    } else {
                        list7.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone7 = false;

                    while (!loopDone7) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone7 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "measurements").equals(reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list7.add(null);
                                    reader.next();
                                } else {
                                    list7.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone7 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setMeasurements((com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.Measurements.class,
                            list7));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "medications").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list8.add(null);
                        reader.next();
                    } else {
                        list8.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Medication.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone8 = false;

                    while (!loopDone8) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone8 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "medications").equals(reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list8.add(null);
                                    reader.next();
                                } else {
                                    list8.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Medication.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone8 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setMedications((com.misyshealthcare.connect.base.clinicaldata.xsd.Medication[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.Medication.class,
                            list8));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "physicalExams").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list9.add(null);

                        reader.next();
                    } else {
                        list9.add(reader.getElementText());
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone9 = false;

                    while (!loopDone9) {
                        // Ensure we are at the EndElement
                        while (!reader.isEndElement()) {
                            reader.next();
                        }

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone9 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "physicalExams").equals(
                                        reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list9.add(null);

                                    reader.next();
                                } else {
                                    list9.add(reader.getElementText());
                                }
                            } else {
                                loopDone9 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setPhysicalExams((java.lang.String[]) list9.toArray(
                            new java.lang.String[list9.size()]));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "planOfCares").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list10.add(null);

                        reader.next();
                    } else {
                        list10.add(reader.getElementText());
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone10 = false;

                    while (!loopDone10) {
                        // Ensure we are at the EndElement
                        while (!reader.isEndElement()) {
                            reader.next();
                        }

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone10 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "planOfCares").equals(reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list10.add(null);

                                    reader.next();
                                } else {
                                    list10.add(reader.getElementText());
                                }
                            } else {
                                loopDone10 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setPlanOfCares((java.lang.String[]) list10.toArray(
                            new java.lang.String[list10.size()]));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "procedures").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list11.add(null);
                        reader.next();
                    } else {
                        list11.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone11 = false;

                    while (!loopDone11) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone11 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "procedures").equals(reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list11.add(null);
                                    reader.next();
                                } else {
                                    list11.add(com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone11 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setProcedures((com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.Procedure.class,
                            list11));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "resolvedProblems").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list12.add(null);
                        reader.next();
                    } else {
                        list12.add(com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem.Factory.parse(
                                reader));
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone12 = false;

                    while (!loopDone12) {
                        // We should be at the end element, but make sure
                        while (!reader.isEndElement())
                            reader.next();

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone12 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "resolvedProblems").equals(
                                        reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list12.add(null);
                                    reader.next();
                                } else {
                                    list12.add(com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem.Factory.parse(
                                            reader));
                                }
                            } else {
                                loopDone12 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setResolvedProblems((com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem[]) org.apache.axis2.databinding.utils.ConverterUtil.convertToArray(
                            com.misyshealthcare.connect.base.clinicaldata.xsd.SimpleProblem.class,
                            list12));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement() &&
                        new javax.xml.namespace.QName(
                            "http://ccd.doc.connect.misyshealthcare.com/xsd",
                            "reviewOfSystems").equals(reader.getName())) {
                    // Process the array and step past its final element's end.
                    nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                            "nil");

                    if ("true".equals(nillableValue) ||
                            "1".equals(nillableValue)) {
                        list13.add(null);

                        reader.next();
                    } else {
                        list13.add(reader.getElementText());
                    }

                    //loop until we find a start element that is not part of this array
                    boolean loopDone13 = false;

                    while (!loopDone13) {
                        // Ensure we are at the EndElement
                        while (!reader.isEndElement()) {
                            reader.next();
                        }

                        // Step out of this element
                        reader.next();

                        // Step to next element event.
                        while (!reader.isStartElement() &&
                                !reader.isEndElement())
                            reader.next();

                        if (reader.isEndElement()) {
                            //two continuous end elements means we are exiting the xml structure
                            loopDone13 = true;
                        } else {
                            if (new javax.xml.namespace.QName(
                                        "http://ccd.doc.connect.misyshealthcare.com/xsd",
                                        "reviewOfSystems").equals(
                                        reader.getName())) {
                                nillableValue = reader.getAttributeValue("http://www.w3.org/2001/XMLSchema-instance",
                                        "nil");

                                if ("true".equals(nillableValue) ||
                                        "1".equals(nillableValue)) {
                                    list13.add(null);

                                    reader.next();
                                } else {
                                    list13.add(reader.getElementText());
                                }
                            } else {
                                loopDone13 = true;
                            }
                        }
                    }

                    // call the converter utility  to convert and set the array
                    object.setReviewOfSystems((java.lang.String[]) list13.toArray(
                            new java.lang.String[list13.size()]));
                } // End of if for expected property start element

                else {
                }

                while (!reader.isStartElement() && !reader.isEndElement())
                    reader.next();

                if (reader.isStartElement()) {
                    // A start element we are not expecting indicates a trailing invalid property
                    throw new org.apache.axis2.databinding.ADBException(
                        "Unexpected subelement " + reader.getLocalName());
                }
            } catch (javax.xml.stream.XMLStreamException e) {
                throw new java.lang.Exception(e);
            }

            return object;
        }
    } //end of factory class
}
